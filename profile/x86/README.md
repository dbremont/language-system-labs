# x86

```bash
as -o add.o add.s        # Assemble
ld -o add add.o          # Link
./add                    # Run

echo $?                  # Print exit code (should be 30)
```

## Instruction Set

> ...
